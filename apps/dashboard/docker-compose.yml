services:

  dashboard:
    container_name: dashboard
    image: lissy93/dashy
    ports:
      - 4000:80
    volumes:
      - ./public:/app/public:ro
      - ../../libs/branding/assets/img:/app/public/img:ro
