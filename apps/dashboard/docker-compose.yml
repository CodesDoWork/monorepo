services:

  dashboard:
    container_name: dashboard-dev
    image: lissy93/dashy
    ports:
      - 4000:80
    volumes:
      - ./public:/app/public
      - ../../libs/branding/assets/img:/app/public/img
