{
    "name": "dashboard",
    "version": "1.0.0",
    "scripts": {
        "build-image": "docker compose build --push dashboard",
        "run-image": "docker compose up -d dashboard",
        "push-image": "docker compose push dashboard"
    }
}
