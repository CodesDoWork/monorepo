{
    "name": "documentation",
    "version": "1.0.0",
    "description": "This package provides documentation for the server",
    "scripts": {
        "build-image": "docker compose build documentation --push",
        "run-image": "docker compose up -d documentation"
    }
}
