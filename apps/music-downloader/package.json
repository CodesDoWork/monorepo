{
    "name": "music-downloader",
    "version": "1.0.0",
    "scripts": {
        "build-image": "docker compose build music-downloader --push",
        "run-image": "docker compose up -d music-downloader"
    }
}
