{
    "name": "certbot",
    "version": "1.0.0",
    "scripts": {
        "build-image": "docker compose -f ../docker-compose.yml build certbot --push",
        "run-image": "docker compose -f ../docker-compose.yml up -d certbot"
    }
}
