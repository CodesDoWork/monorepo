{
    "name": "dashboard",
    "version": "1.0.0",
    "scripts": {
        "build-image": "docker compose -f ../docker-compose.yml build dashboard --push",
        "run-image": "docker compose -f ../docker-compose.yml up -d dashboard"
    }
}
