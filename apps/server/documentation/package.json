{
    "name": "documentation",
    "version": "1.0.0",
    "description": "This package provides documentation for the server",
    "scripts": {
        "build-image": "cross-env COMPOSE='docker compose -f ../docker-compose.yml' SERVICE=documentation npm run build-and-push",
        "build-and-push": "cross-env-shell '$COMPOSE build --builder container $SERVICE && $COMPOSE push $SERVICE'",
        "run-image": "docker compose -f ../docker-compose.yml up -d documentation"
    }
}
