{
    "name": "server-music",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "projectType": "application",
    "targets": {
        "pre-docker": {
            "executor": "nx:run-commands",
            "options": {
                "command": "touch ./apps/server/music/.env"
            }
        },
        "lint": {
            "executor": "nx:run-commands",
            "options": {
                "command": "echo no linting"
            }
        }
    },
    "tags": []
}
