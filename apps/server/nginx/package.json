{
    "name": "nginx",
    "version": "1.0.0",
    "scripts": {
        "build-image": "docker compose -f ../docker-compose.yml build nginx --push",
        "run-image": "docker compose -f ../docker-compose.yml up -d nginx"
    }
}
