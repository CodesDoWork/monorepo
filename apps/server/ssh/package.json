{
    "name": "ssh",
    "version": "1.0.0",
    "scripts": {
        "build-image": "docker compose -f ../docker-compose.yml build ssh --push",
        "run-image": "docker compose -f ../docker-compose.yml up -d ssh"
    }
}
