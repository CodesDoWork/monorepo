{
    "name": "server-scripts",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "projectType": "library",
    "targets": {
        "deploy-command": {
            "executor": "nx:run-commands",
            "options": {
                "command": "echo chmod 755 /home/jkonratt"
            }
        },
        "lint": {
            "executor": "nx:run-commands",
            "options": {
                "command": "echo no linting"
            }
        }
    },
    "tags": []
}
