openssl req \
    -newkey rsa:4096 -nodes -sha256 -keyout server.key \
    -addext "subjectAltName = DNS:justin.konratts.de" \
    -x509 -days 3650 -out server.crt \
    -subj "/CN=justin.konratts.de"
