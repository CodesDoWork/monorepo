secrets:
  ../index:
    prefix: false
    vars:
      - SERVER_ADDRESS

  ../ssh:
    prefix: true
    vars:
      - USERNAME
      - HOST
      - ROOT_DESTINATION
      - name: PRIVATE_KEY
        file: ./.ssh/id_rsa
      - name: KNOWN_HOSTS
        file: ./.ssh/known_hosts
