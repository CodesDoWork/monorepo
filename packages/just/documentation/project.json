{
    "name": "just-documentation",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "projectType": "application",
    "targets": {
        "pre-build": {
            "executor": "nx:run-commands",
            "options": {
                "commands": ["typedoc --out ./packages/just/documentation/docs/project"]
            }
        }
    },
    "tags": ["app"]
}
