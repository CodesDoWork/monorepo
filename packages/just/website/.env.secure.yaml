secrets:
  ../index:
    vars:
      - DOMAIN
      - URL

  ../cms:
    prefix: true
    vars:
      - URL

  index:
    vars:
      - CMS_TOKEN
      - PORT
      - CONTACT_EMAIL

  ../smtp:
    prefix: true
    vars:
      - USERNAME
      - PASSWORD
      - HOST
      - PORT
