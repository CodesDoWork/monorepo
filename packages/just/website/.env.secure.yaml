secrets:
  ../index:
    vars:
      - DOMAIN
      - URL

  ../cms:
    prefix: true
    vars:
      - URL

  index:
    vars:
      - GRAVATAR_EMAIL
      - CMS_TOKEN
      - PORT

  ../smtp:
    prefix: true
    vars:
      - USERNAME
      - PASSWORD
      - HOST
      - PORT
