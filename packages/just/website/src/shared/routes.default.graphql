fragment MinimalTranslatedRoute on just_site_routes {
    id
    translations {
        language: languages_code {
            code
            short
            isFallback: is_fallback
        }
        route
    }
}
