# nx-containers

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nx-containers` to build the library.

## Running unit tests

Run `nx test nx-containers` to execute the unit tests via [Jest](https://jestjs.io).
