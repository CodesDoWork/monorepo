{
    "$schema": "https://json-schema.org/schema",
    "version": 2,
    "title": "Build executor",
    "description": "",
    "type": "object",
    "properties": {
        "service": {
            "type": "string",
            "description": "Service name"
        },
        "config": {
            "type": "string",
            "description": "Where to find the docker compose config (dir or file path)"
        }
    },
    "required": []
}
