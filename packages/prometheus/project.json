{
    "name": "prometheus",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/prometheus",
    "projectType": "application",
    "targets": {
        "build-image": {
            "executor": "nx:run-commands",
            "options": {
                "command": "docker compose build prometheus"
            }
        },
        "push-image": {
            "executor": "nx:run-commands",
            "options": {
                "command": "docker compose push prometheus"
            }
        },
        "trivy": {
            "executor": "@codesdowork/workspace-plugin:trivy"
        }
    },
    "tags": ["shared"]
}
