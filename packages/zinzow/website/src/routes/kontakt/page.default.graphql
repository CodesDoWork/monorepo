query GetContactData {
    contact {
        name
        addressLine1
        addressLine2
        tel
        email
        acceptPrivacyPolicy: accept_privacy_policy
        contactPhoto: contact_photo {
            ...DirectusImage
        }
        coordinates
    }
}
