query GetContactData {
    contact {
        title
        intro

        name
        addressLine1
        addressLine2
        tel
        email

        contactPhoto: contact_photo {
            ...DirectusImage
        }

        acceptPrivacyPolicy: accept_privacy_policy

        findUs: find_us
        coordinates
    }
}
