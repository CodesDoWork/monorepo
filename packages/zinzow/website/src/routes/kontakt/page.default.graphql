query GetContactData {
    contact {
        name
        addressLine1
        addressLine2
        tel
        email
        contactPhoto: contact_photo {
            ...DirectusImage
        }
        coordinates
    }
}
