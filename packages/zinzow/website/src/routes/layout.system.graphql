query GetLayoutSystemData {
    settings {
        project_name
        project_descriptor
        project_color
        secondaryColor
        accentColor
        copyright
    }

    logo: files(filter: { isLogo: { _eq: true } }) {
        id
    }
}
