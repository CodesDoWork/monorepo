query GetLayoutSystemData {
    settings {
        project_name
        project_descriptor
        project_color
        backgroundColor: background_color
        logoBackgroundColor
        copyright
    }
}
