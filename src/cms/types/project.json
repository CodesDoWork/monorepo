{
    "name": "cms-types",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "src/cms/types/src",
    "projectType": "library",
    "tags": ["shared"],
    "targets": {
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "src/cms/types/jest.config.ts"
            }
        }
    }
}
