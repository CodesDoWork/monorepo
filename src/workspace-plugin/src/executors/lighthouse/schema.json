{
    "$schema": "https://json-schema.org/schema",
    "version": 2,
    "title": "Lighthouse executor",
    "description": "",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "Project name"
        },
        "urls": {
            "type": "array",
            "description": "URLs to analyze"
        }
    },
    "required": ["project", "urls"]
}
